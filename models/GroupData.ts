// data while fetching group summary info
export type GroupSummaryView = {
    GROUP_ID: number,
    NAME: string,
    GROUP_MEMBERS_COUNT: number,
    MEMBER_ROLE: string,
    GROUP_POSTS_COUNT: number
}