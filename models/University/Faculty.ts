export default class Faculty {
    name: string;
    designation: string;

    constructor(name: string, designation: string) {
        this.name = name;
        this.designation = designation;
    }
}